import { ActivateMfaHandlerResult, ActivateMfaState, GenerateQrCodeHandlerResult, GenerateQrCodeState } from '@nhost/nhost-js';
import { ToRefs } from 'vue';

export interface ConfigMfaComposableState extends ToRefs<ActivateMfaState>, ToRefs<GenerateQrCodeState> {
    generateQrCode: () => Promise<GenerateQrCodeHandlerResult>;
    activateMfa: (code: string) => Promise<ActivateMfaHandlerResult>;
}
/**
 * @internal
 * TODO documentation when available in Nhost Cloud - see changelog
 * TODO use a common reactive 'result' state - see for instance useChangeEmail
 * TODO use the promise action
 * @returns
 */
export declare const useConfigMfa: () => ConfigMfaComposableState;
//# sourceMappingURL=useConfigMfa.d.ts.map