import { Ref } from 'vue';

/**
 * Use `useAccessToken` to get the access token of the user.
 *
 * @example
 * ```ts
 * const accessToken = useAccessToken();
 * ```
 *
 * @docs https://docs.nhost.io/reference/vue/use-access-token
 */
export declare const useAccessToken: () => Ref<string | null>;
//# sourceMappingURL=useAccessToken.d.ts.map